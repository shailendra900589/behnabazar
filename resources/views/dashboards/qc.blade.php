@extends('layouts.dashboard')
@section('title','QC Dashboard')
@section('dashboard')
<h1 class="fw-bold">QC Dashboard</h1><p class="text-muted mb-4">Review vendor uploads and keep the storefront professional.</p><div class="row g-4"><div class="col-xl-8"><div class="table-card"><div class="p-4 border-bottom"><h4 class="fw-bold mb-0">Pending Review</h4></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Product</th><th>Vendor</th><th>Price</th><th>Decision</th></tr></thead><tbody>@forelse($pending as $product)<tr><td>{{ $product->title }}<div class="small text-muted">{{ $product->category->name ?? '' }}</div></td><td>{{ $product->vendor->shop_name ?? 'Official' }}</td><td>₹{{ number_format($product->price,2) }}</td><td><form data-ajax-form data-method="PATCH" data-reload="true" action="{{ route('manage.products.review',$product) }}" class="d-flex gap-2"><input type="hidden" name="decision" value="approved"><button class="btn btn-success btn-sm">Approve</button></form><form method="post" action="{{ route('manage.products.review',$product) }}" class="d-flex gap-2 mt-2">@csrf @method('PATCH')<input type="hidden" name="decision" value="rejected"><input class="form-control form-control-sm" name="reject_reason" placeholder="Reason"><button class="btn btn-outline-danger btn-sm">Reject</button></form></td></tr>@empty<tr><td colspan="4" class="text-center text-muted py-4">All caught up.</td></tr>@endforelse</tbody></table></div></div></div><div class="col-xl-4"><div class="bb-card p-4"><h4 class="fw-bold">Team</h4>@foreach($team as $member)<div class="d-flex justify-content-between border-bottom py-2"><span>{{ $member->name }}</span><span class="badge badge-soft">{{ str_replace('_',' ',$member->role) }}</span></div>@endforeach</div></div></div>
@endsection
